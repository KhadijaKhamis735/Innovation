package com.example.Innovation_backend.security;

/**
 * THIS CLASS IS INTENTIONALLY EMPTY (only a marker for removal).
 *
 * It was a Phase 1 placeholder. Phase 2 introduced
 * {@link com.example.Innovation_backend.user.UserDetailsServiceImpl}
 * which loads users from the database.
 *
 * If this file still exists in your project, you can safely delete it.
 * Spring will inject UserDetailsServiceImpl for the
 * auth.manager.user-details-service wiring.
 */
@Deprecated
final class CustomUserDetailsService {
    private CustomUserDetailsService() {}
}
